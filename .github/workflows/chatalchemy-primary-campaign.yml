name: ChatAlchemy Frozen Primary Campaign

on:
  push:
    branches:
      - paper-campaign-run-2026-08-19

permissions:
  contents: read

concurrency:
  group: chatalchemy-frozen-primary-refs/heads/paper-campaign-run-2026-08-19-test-gpt-5.1-2025-11-13
  cancel-in-progress: true

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Campaign paused safely
        run: |
          echo "Frozen primary campaign paused: OpenAI model preflight returned HTTP 403."
          echo "No paper result should be interpreted from the aborted campaign."
